# Backend Changelog

## 0.1.0

### Added
- OpenAPI v1 contract generation from Zod schemas (`backend/openapi/openapi.v1.json`).
- Contract drift guard script and CI enforcement.
- API response contract tests using Zod.
- Versioned API routing (`/api/v1`) with deprecated `/api` migration path.

